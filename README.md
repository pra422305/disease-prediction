# 🏥 Medical Disease Prediction System

> An end-to-end, production-ready AI application that predicts diseases from symptoms using Random Forest and XGBoost classifiers — deployable on Streamlit Community Cloud with zero configuration.

---

## 📌 Project Overview

**MedAI** is a full-stack machine-learning web application that:

- Accepts up to **5+ user-selected symptoms**
- Predicts the most likely **disease** with a **confidence score**
- Shows **top-5 probable diseases** with probability bars
- Provides complete **disease information** — description, causes, prevention, and recommended specialist
- Displays rich **analytics** — feature importance, confusion matrix, heatmaps, and Plotly charts

---

## ✨ Features

| Feature | Details |
|---------|---------|
| 🩺 Disease Prediction | Symptom-based instant prediction with confidence gauge |
| 📚 Disease Library | 41 diseases with detailed medical information |
| 📊 Model Analytics | Matplotlib, Seaborn, and Plotly visualisations |
| 🤖 Dual ML Models | Random Forest + XGBoost with automatic best-model selection |
| ☁️ Cloud-Ready | Streamlit Community Cloud compatible |
| 🎨 Dark UI | Custom CSS dark theme with responsive layout |
| 📝 Logging | All events logged to `logs/app.log` |
| ✅ Validation | Input validation and graceful error handling |

---

## 📁 Folder Structure

```
Medical-Disease-Prediction/
├── app.py                          ← Main Streamlit entry point
├── requirements.txt
├── README.md
├── model/
│   ├── train_model.py              ← Model training script
│   ├── disease_prediction_model.pkl
│   ├── label_encoder.pkl
│   ├── symptoms_list.pkl
│   ├── model_metrics.json
│   └── cm_data.pkl
├── data/
│   └── disease_dataset.csv
├── utils/
│   ├── predictor.py                ← Prediction logic
│   ├── preprocessing.py            ← Feature engineering
│   ├── logger.py                   ← Logging setup
│   └── visualizations.py           ← All charts
├── pages/
│   ├── 1_Disease_Prediction.py
│   ├── 2_Disease_Information.py
│   ├── 3_Model_Analytics.py
│   └── 4_About.py
├── assets/
│   └── styles.css
├── disease_info/
│   └── disease_details.json
└── logs/
    └── app.log
```

---

## 🚀 Installation & Run Locally

### Prerequisites
- Python 3.9 or higher
- pip

### Steps

```bash
# 1. Clone the repository
git clone https://github.com/your-username/Medical-Disease-Prediction.git
cd Medical-Disease-Prediction

# 2. Create a virtual environment (recommended)
python -m venv venv
source venv/bin/activate        # Linux / macOS
venv\Scripts\activate           # Windows

# 3. Install dependencies
pip install -r requirements.txt

# 4. Run the app (model trains automatically on first launch)
streamlit run app.py
```

The app automatically trains the model on first launch if the `.pkl` files are missing.

---

## 🧠 Manual Training (Optional)

To retrain the model explicitly:

```bash
python model/train_model.py
```

This will:
1. Load `data/disease_dataset.csv`
2. Train **Random Forest** and **XGBoost** classifiers
3. Evaluate both models and select the best
4. Save artefacts to `model/`

---

## ☁️ Deploy to Streamlit Community Cloud

1. Push your project to a **public GitHub repository**
2. Visit [share.streamlit.io](https://share.streamlit.io)
3. Click **New app** → select your repo
4. Set **Main file path** to `app.py`
5. Click **Deploy** — done! 🎉

The app auto-trains the model on first launch (no pre-built `.pkl` files needed).

---

## 📊 Machine Learning Details

| Item | Details |
|------|---------|
| Models | Random Forest, XGBoost |
| Features | Binary one-hot encoded symptom vector |
| Target | Disease label (41 classes) |
| Split | 80% train / 20% test (stratified) |
| Selection | Best model by weighted F1 score |
| Persistence | Pickle (.pkl) + JSON |

---

## 🖼️ Screenshots

> _Add screenshots of each page here after deployment._

| Page | Description |
|------|-------------|
| Home | Welcome banner and feature overview |
| Disease Prediction | Symptom selection, result card, gauge, and probability chart |
| Disease Information | Searchable disease library with severity filters |
| Model Analytics | Matplotlib + Seaborn + Plotly charts |
| About | Project info and tech stack |

---

## ⚠️ Disclaimer

This project is built for **educational and demonstration purposes only**.  
The predictions are generated by a machine-learning model trained on a simplified dataset and should **never** be used as a substitute for professional medical advice, diagnosis, or treatment.  
Always consult a licensed healthcare professional for any health concerns.

---

## 📄 License

MIT License — free to use, modify, and distribute for educational purposes.
